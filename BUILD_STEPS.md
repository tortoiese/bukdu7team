# ENTRY 개발·배포 현황

이 문서는 초기 구축 계획이 아니라 현재 구현 상태와 남은 작업 순서를 기록합니다. 기준일은 2026년 8월 21일입니다.

## 1. 현재 완료된 작업

### 프로젝트 기반

- [x] React 19 + TypeScript + Vite 프론트엔드 구성
- [x] Spring Boot 3.5.16 + Java 21 백엔드 구성
- [x] 로컬 H2와 배포용 PostgreSQL 프로필 분리
- [x] 공통 디자인 토큰과 재사용 컴포넌트 구성
- [x] 공통 API 응답·오류 형식 구성
- [x] Swagger UI와 OpenAPI 문서 노출

### 백엔드 기능

- [x] 익명 세션 발급·현재 세션 조회·시장 변경
- [x] 무효 세션 자동 교체와 응답 헤더 동기화
- [x] 제품·재고 조회와 스캔 이벤트 기록
- [x] 아카이브 저장·삭제·조회
- [x] 팝업 패스포트 발급·조회·존 스탬프
- [x] 방문 리캡과 계정 연결 모의 처리
- [x] 시장별 국경 이전 정보 조회
- [x] P6 구매 고민 대화와 3턴 제한
- [x] Anthropic 연동, 모의 AI, 규칙 기반 대체 응답
- [x] 입력 검증·세션 소유권·동시 요청 통합 테스트

### 프론트엔드 기능

- [x] API 연결 상태 화면
- [x] P1 스캔 결과 화면
- [x] P6 구매 고민 대화 화면
- [x] 세션 발급·자동 교체 처리
- [x] 디자인 시스템 확인 화면

## 2. 남은 해커톤 우선 작업

### 1순위: P0 사용자 흐름 완성

1. P2 팝업 패스포트 화면을 기존 API에 연결
2. P3 방문 리캡 화면을 기존 API에 연결
3. P4 국경 이전 화면을 기존 API에 연결
4. P5 아카이브 화면을 기존 API에 연결
5. P1 → P2 → P3 → P4 흐름을 모바일 실기기에서 점검

### 2순위: 시연 안정화

1. 실제 배포 주소로 CORS와 `VITE_API_BASE` 설정
2. 실제 Anthropic 키 사용 여부 결정
3. API 장애·AI 장애 시 대체 응답 확인
4. 시연용 QR을 배포된 프론트엔드 주소로 생성
5. 발표 직전 세션 초기화와 시연 순서 리허설

### 3순위: 시간이 남을 때

- P8 사전 등록
- D1 운영자 대시보드
- P7 어드바이저 화면
- D2 페르소나 콘솔

P7과 D2는 핵심 사용자 흐름에 필요하지 않으므로 일정이 부족하면 제외합니다.

## 3. 로컬 실행

### 프론트엔드

```bash
cd web
npm install
npm run dev
```

### 백엔드

macOS·Linux:

```bash
cd api
./gradlew bootRun
```

Windows:

```powershell
cd api
.\gradlew.bat bootRun
```

프론트엔드와 백엔드는 터미널을 각각 하나씩 열어 실행합니다.

## 4. 개발 완료 기준

### 프론트엔드

```bash
cd web
npm run lint
npm run build
```

### 백엔드

```bash
cd api
./gradlew clean test
```

추가로 다음 항목을 확인합니다.

- [ ] API 변경 사항이 `docs/API_CONTRACT.md`에 반영됨
- [ ] 모바일 402×874 기준에서 주요 화면이 정상 표시됨
- [ ] 익명 세션으로 모든 핵심 기능을 사용할 수 있음
- [ ] AI 키가 없어도 대화가 중단되지 않음
- [ ] 할인·쿠폰·가격 경쟁 문구가 없음
- [ ] API 키와 개인정보가 저장소에 포함되지 않음

## 5. 배포 예정 구성

### 데이터베이스

- Neon PostgreSQL 프로젝트 생성
- 연결 정보를 Railway 환경 변수로 등록

### 백엔드

- Railway에서 저장소의 `api` 폴더를 배포
- 필수 환경 변수:

| 변수 | 설명 |
|---|---|
| `SPRING_PROFILES_ACTIVE` | `prod` |
| `SPRING_DATASOURCE_URL` | PostgreSQL JDBC 주소 |
| `SPRING_DATASOURCE_USERNAME` | 데이터베이스 사용자 |
| `SPRING_DATASOURCE_PASSWORD` | 데이터베이스 비밀번호 |
| `ENTRY_CORS_ORIGINS` | 배포된 프론트엔드 주소 |
| `ENTRY_AI_MOCK` | 실제 AI 사용 시 `false` |
| `ANTHROPIC_API_KEY` | 실제 AI 사용 시 설정 |

배포 후 `/api/v1/health`와 `/swagger-ui.html`을 확인합니다.

### 프론트엔드

- Vercel에서 저장소의 `web` 폴더를 배포
- `VITE_API_BASE`를 배포된 백엔드의 `/api/v1` 주소로 설정
- SPA 새로고침을 위한 재작성 규칙 설정

## 6. 배포 점검표

- [ ] 브라우저에 CORS 오류가 없음
- [ ] 프론트엔드와 백엔드가 모두 HTTPS를 사용함
- [ ] 시크릿 창에서 세션 발급이 성공함
- [ ] 모바일에서 QR 스캔 후 P1 화면이 열림
- [ ] P1에서 저장과 P6 대화가 동작함
- [ ] 무효 세션이 자동으로 새 세션으로 교체됨
- [ ] AI 호출 실패 시 규칙 기반 응답이 표시됨
- [ ] 발표 10분 전에 백엔드 헬스체크를 호출함

## 7. 협업 방식

- `main`에 직접 커밋하지 않습니다.
- 기능 또는 문서별 작업 브랜치를 만듭니다.
- PR에서 변경 내용과 검사 결과를 공유합니다.
- 팀원 정보처럼 여러 사람이 동시에 수정할 수 있는 내용은 `docs/team/이름.md`로 파일을 나눕니다.
