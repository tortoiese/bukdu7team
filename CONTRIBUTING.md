# ENTRY 기여 안내

## 기본 원칙

- `main` 브랜치에 직접 커밋하지 않습니다.
- 작업마다 개인 브랜치를 만들고 PR로 반영합니다.
- 한 PR에는 한 가지 목적의 변경만 담습니다.
- API를 바꾸면 같은 PR에서 `docs/API_CONTRACT.md`도 수정합니다.
- 문서의 제목과 설명은 한국어로 작성합니다. 코드 식별자와 공식 기술명은 원문을 유지합니다.

## GitHub 웹에서 수정하는 방법

Git을 설치하거나 저장소를 복제하지 않아도 됩니다.

1. GitHub에서 수정할 파일을 엽니다.
2. 오른쪽 위 연필 아이콘을 누릅니다.
3. 내용을 수정하고 `Commit changes`를 누릅니다.
4. `Create a new branch for this commit and start a pull request`를 선택합니다.
5. 브랜치 이름을 `docs/이름-작업내용`처럼 작성합니다.
6. 커밋을 만든 뒤 PR 제목과 설명을 작성합니다.
7. 팀원 검토를 받은 다음 `main`에 병합합니다.

`Commit directly to the main branch`는 선택하지 않습니다.

## 팀원 정보 추가 방법

여러 명이 `README.md`의 같은 부분을 동시에 고치면 충돌이 생길 수 있습니다. 각자 별도 파일을 만드는 방식을 사용합니다.

1. 저장소에서 `Add file` → `Create new file`을 선택합니다.
2. 파일명을 `docs/team/이름.md`로 입력합니다.
3. 아래 양식으로 작성합니다.

```markdown
# 이름

- 담당: 백엔드
- 한 줄 소개: ENTRY의 세션과 API 개발을 담당합니다.
```

4. 개인 브랜치를 만들고 PR을 생성합니다.

이 방식은 팀원끼리 수정 위치가 겹치지 않아 충돌 가능성이 낮습니다.

## PR 확인 사항

- [ ] 변경 목적이 제목에 드러나는가
- [ ] `main`이 아닌 개인 브랜치에서 작업했는가
- [ ] 관련 문서를 함께 수정했는가
- [ ] 프론트엔드 변경 시 `npm run lint`, `npm run build`가 성공하는가
- [ ] 백엔드 변경 시 `./gradlew clean test`가 성공하는가
- [ ] API 키나 개인정보가 포함되지 않았는가
